import { StudyUnitReference } from '../../model/studyUnitReference';

// export default function getOperation(id: string): Promise<StudyUnitReference> {
//   return fetch(`${import.meta.env.VITE_API_BASE_URL}api/other/${id}`).then(
//     (response) => {
//       const data = response.json();
//       console.log('Statistical Operation Data: ', data);
//     }
//   );
// }
